<%= form_for(current_user.favorites.find_by(micropost_id: micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfavo", class: "btn" %>
<% end %>